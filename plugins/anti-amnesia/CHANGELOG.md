# Changelog

## 0.2.0 - 2026-09-05

- Embedded the shared handoff receipt from `contracts/handoff-receipt.md`; the closing receipt is now the same `RECEIPT` block that `cleancoding` writes and that later retrospective answers are read from.
- Added a Claude Code manifest and a README; hook commands resolve `python` or `python3`.
- Added `metadata.version` to the skill frontmatter.

## 0.1.0 - 2026-08-26

- First marketplace release: retrospective recall policy with a `SessionStart` hook.
