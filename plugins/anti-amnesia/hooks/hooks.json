{
  "description": "Keep recent-work recall and the receipt policy available across session starts, resumes, clears, and compaction.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python >/dev/null 2>&1; then python \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py\"; else python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py\"; fi",
            "commandWindows": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py\"",
            "timeout": 10,
            "statusMessage": "Loading recent-work recall policy",
            "additionalContextLimit": 700
          }
        ]
      }
    ]
  }
}
